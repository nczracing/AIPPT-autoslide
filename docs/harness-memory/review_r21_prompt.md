# AutoSlide 第21轮独立代码审查请求

你是独立代码审查者。请审查 E:/study/projects/autoslide/core/pptx_builder.py 中刚完成的"文字安全边距优化"改动（第20轮审查已通过，本轮是其后续迭代）。

## 审查背景

用户预览生成物后要求："文字不要出现在画面边缘"。目标：所有含文字元素与幻灯片四边保持 ≥0.3" 安全边距，且边距标准全布局统一。

## 本次改动点（供定位）

1. `_add_footer`：页脚水印 (0.4,7.05,6.0,0.4) → (0.7,6.95,4.0,0.25)，底边距 0.05"→0.30"
2. `_set_title`：标题占位符显式设为 (0.7,0.45,11.9,0.95)，替换模板默认 (0.5,0.3) 贴边位置
3. `_content_textbox`：正文框高度 5.6→5.45（底边 7.1→6.95，底边距 0.55"）
4. `_fill_two_content`：无图双栏显式设安全区几何（左右0.7"对称、栏间距0.4"），替换模板默认 0.5" 边距与 9.5" 右截断
5. `_adjust_textbox_for_image`：left/right/top 三模式文字区底边距统一 0.55"（margin_bottom 常量）
6. `_fit_registered_text`：估算余量加严 avail_w-0.3 / avail_h-0.25；`_estimate_body_height` 基数 0.1→0.15

## 验证结果（已完成）

- py_compile 通过
- _test_layout.py 10 场景通过（含新增 V5 检查：所有含文字形状四边距 ≥0.3"，容差 0.05）
- _test_export.py 5/5 通过
- 演示文件 7 页贴边自检 0 问题；exe 冒烟测试通过

## 审查要求

- 禁止修改代码、禁止输出完整替代实现、禁止扩大需求、禁止风格重构
- 必须区分真正风险 / 优化建议 / 个人偏好
- 输出格式：
  - Overall Result: PASS / FAIL
  - Risk Summary: P0/P1/P2/P3 分级列出
  - Findings: 每条含 编号(F-21-XX)、等级、文件:行号、描述、建议
