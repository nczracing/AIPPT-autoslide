你是一名独立的代码审查专家。请对以下AutoSlide项目的PPT图片布局和动画优化进行审查。

## 背景
- 项目路径：E:/study/projects/autoslide
- 本轮功能：图片布局适配 + 动画效果
- 核心改动：
  1. 图片保持原始比例，不再硬编码尺寸
  2. 支持right/left/top三种布局模式
  3. 文本框自适应避免重叠
  4. 页面切换动画和文字出现动画

## 审查重点

### 1. 图片布局逻辑
- `_calc_image_position()` 是否正确处理了各种宽高比？
- 方形图(1:1)、宽图(3:2)、高图(2:3)的尺寸计算是否正确？
- 位置计算是否避免超出幻灯片边界？
- 垂直居中逻辑是否可靠？

### 2. 动画注入
- `sldTrans` XML结构是否符合Open XML规范？
- `animCue`/`anim` 动画元素是否正确注入？
- 动画是否可能导致PPT打开失败或崩溃？
- 触发条件（click/timed）设置是否合理？

### 3. 边界情况
- 图片文件不存在时的处理
- PIL读取失败时的fallback
- img_ratio为0或无穷大的保护
- 文本框为空时的容错

### 4. 代码质量
- 方法职责是否单一？
- 错误处理是否完整？
- 日志记录是否充分？
- 命名是否清晰？

## 输出要求
请用中文回答，格式如下：

### 整体评估
PASS / FAIL / PASS with warnings

### 问题摘要（按严重度分组）
**P0 - 严重问题（必须修复）**
- [问题描述]

**P1 - 重要问题（建议修复）**
- [问题描述]

**P2 - 一般问题（可选修复）**
- [问题描述]

**P3 - 建议优化（可选）**
- [问题描述]

### 详细发现
对每个发现的问题，提供：
1. 问题位置（文件:行号）
2. 问题描述
3. 风险等级
4. 修复建议

### 验证建议
说明如何通过测试验证修复效果。
